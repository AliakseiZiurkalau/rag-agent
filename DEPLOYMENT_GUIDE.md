# 🚀 Руководство по деплою на локальный сервер

## Информация о сервере

- **IP:** 192.168.100.25
- **Hostname:** localpiserver
- **User:** admin
- **Path:** /home/admin/rag-agent

## Быстрый старт

### Шаг 1: Проверка SSH доступа

```bash
# Проверьте подключение
ssh admin@192.168.100.25

# Если нужно, настройте SSH ключ
ssh-copy-id admin@192.168.100.25
```

### Шаг 2: Первоначальная настройка сервера

```bash
# Запустите один раз для настройки сервера
./deploy/setup_server.sh
```

Установит:
- Python 3, Node.js
- Ollama + модель llama3.2:1b
- Системные пакеты

### Шаг 3: Деплой приложения

```bash
# Деплой на сервер
./deploy/deploy.sh
```

### Шаг 4: Проверка

Откройте в браузере:
- **Frontend:** http://192.168.100.25:3000
- **Backend:** http://192.168.100.25:8000

## Управление

### Просмотр логов

```bash
# Backend логи
ssh admin@192.168.100.25 'sudo journalctl -u rag-agent-backend -f'

# Frontend логи
ssh admin@192.168.100.25 'sudo journalctl -u rag-agent-frontend -f'
```

### Перезапуск

```bash
# Перезапустить все сервисы
ssh admin@192.168.100.25 'sudo systemctl restart rag-agent-*'
```

### Статус

```bash
# Проверить статус
ssh admin@192.168.100.25 'sudo systemctl status rag-agent-*'
```

## Обновление

После изменений в коде:

```bash
# 1. Закоммитьте изменения
git add .
git commit -m "Update"

# 2. Запустите деплой
./deploy/deploy.sh
```

## Полная документация

См. [deploy/README.md](deploy/README.md) для:
- Детальных инструкций
- Troubleshooting
- Мониторинга
- Резервного копирования
- Безопасности

---

**Версия:** 2.4.0
