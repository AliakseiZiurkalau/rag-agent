# 🔧 Устранение проблем

## Быстрая диагностика

```bash
# Проверка здоровья системы
curl http://localhost:8000/health

# Проверка Ollama
ollama list

# Проверка логов
tail -f logs/rag_agent.log

# Проверка портов
lsof -i :8000  # Backend
lsof -i :3000  # Frontend
lsof -i :11434 # Ollama
```

## 🔥 Частые проблемы

### 1. Gemini возвращает пустой ответ

**Симптомы:**
```
Ошибка генерации через gemini: Модель вернула пустой ответ
```

**Причина:** Gemini 2.0+ использует "thinking mode" и тратит токены на размышления.

**Решение:**
- Система автоматически использует 2000 токенов для Gemini
- Попробуйте `gemini-2.0-flash-exp` вместо `gemini-1.5-flash`
- Или используйте `gemini-1.5-pro` (стабильная версия)

**Проверка:**
```bash
curl -X POST "http://localhost:8000/query" \
  -H "Content-Type: application/json" \
  -d '{"question": "Test question"}'
```

---

### 2. Ollama не запускается

**Симптомы:**
```
Connection refused: http://localhost:11434
Ошибка подключения к Ollama
```

**Решение:**
```bash
# Проверить статус
ollama list

# Запустить сервис
ollama serve &

# Проверить порт
lsof -i :11434

# Если порт занят
killall ollama
ollama serve &
```

**macOS:**
```bash
brew services restart ollama
```

**Linux:**
```bash
systemctl restart ollama
```

---

### 3. Модель не найдена

**Симптомы:**
```
404 Client Error: Not Found
Model 'llama3.2:1b' not found
```

**Решение:**
```bash
# Проверить установленные модели
ollama list

# Скачать модель
ollama pull llama3.2:1b

# Проверить снова
ollama list
```

---

### 4. Медленная генерация

**Причина:** Большие параметры или слабое железо.

**Решение 1: Оптимизация параметров**
```
Settings → System → Generation Parameters:
- Temperature: 0.1-0.3
- Max Tokens: 80-150
- Context Size: 512-1024
- Context Length: 300-500
```

**Решение 2: Лёгкая модель**
```bash
ollama pull llama3.2:1b  # 1.3 GB, самая быстрая
```

**Решение 3: API модель**
```
Settings → System → API Configuration:
- OpenAI: gpt-4o-mini (быстрая и дешёвая)
- Gemini: gemini-2.0-flash-exp (бесплатная)
```

---

### 5. API ключ не работает

**OpenAI:**
```bash
# Проверка ключа
curl https://api.openai.com/v1/models \
  -H "Authorization: Bearer YOUR_API_KEY"

# Проверка баланса
https://platform.openai.com/usage
```

**Gemini:**
```bash
# Проверка ключа
curl "https://generativelanguage.googleapis.com/v1/models?key=YOUR_API_KEY"

# Активация API
https://aistudio.google.com/apikey
```

**Anthropic:**
```bash
# Проверка ключа
curl https://api.anthropic.com/v1/messages \
  -H "x-api-key: YOUR_API_KEY" \
  -H "anthropic-version: 2023-06-01" \
  -H "Content-Type: application/json" \
  -d '{"model":"claude-3-5-sonnet-20241022","max_tokens":10,"messages":[{"role":"user","content":"Hi"}]}'
```

**Частые ошибки:**
- ❌ Неправильный формат ключа
- ❌ Нет средств на аккаунте
- ❌ Превышен лимит запросов
- ❌ Неправильное название модели

---

### 6. Документы не загружаются

**Симптомы:**
```
Ошибка загрузки файла
File too large
Unsupported format
```

**Проверка:**
- ✅ Формат: PDF, DOCX, XLSX, XLS
- ✅ Размер: до 50 MB
- ✅ Содержимое: текст (не только изображения)

**Решение:**
```bash
# Проверить логи
tail -f logs/rag_agent.log

# Проверить права
ls -la data/documents/

# Очистить кэш
rm -rf data/documents/.cache
```

**Для PDF с изображениями:**
- Используйте OCR для извлечения текста
- Или конвертируйте в текстовый формат

---

### 7. XWiki импорт не работает

**Симптомы:**
```
Connection error
Authentication failed
No pages found
```

**Решение:**

**Проверка URL:**
```bash
# Правильный формат
http://localhost:8080/xwiki

# Проверка доступности
curl http://localhost:8080/xwiki/rest
```

**Проверка аутентификации:**
```bash
# С credentials
curl -u username:password http://localhost:8080/xwiki/rest/wikis/xwiki/spaces
```

**Проверка REST API:**
1. Откройте XWiki в браузере
2. Перейдите: Administration → Programming → REST API
3. Убедитесь, что API включен

---

### 8. Web Import не работает

**Симптомы:**
```
Invalid URL
Connection timeout
Too many pages
```

**Решение:**

**Проверка URL:**
```bash
# URL должен начинаться с http:// или https://
curl -I https://example.com
```

**Проверка доступности:**
```bash
# Проверить, что сайт доступен
curl -L https://example.com
```

**Ограничения:**
- Максимум 100 страниц за раз
- Некоторые сайты блокируют скрапинг
- Требуется стабильное интернет-соединение

---

### 9. Frontend не загружается

**Симптомы:**
```
Cannot GET /
Connection refused: http://localhost:3000
```

**Решение:**
```bash
# Проверить процесс
ps aux | grep "npm run dev"

# Перезапустить frontend
cd frontend
npm run dev

# Очистить кэш
rm -rf node_modules package-lock.json
npm install
npm run dev
```

**Проверка портов:**
```bash
# Если порт 3000 занят
lsof -i :3000
kill -9 <PID>
```

---

### 10. Backend не отвечает

**Симптомы:**
```
Connection refused: http://localhost:8000
502 Bad Gateway
```

**Решение:**
```bash
# Проверить процесс
ps aux | grep "python main.py"

# Перезапустить backend
source venv/bin/activate
python main.py

# Проверить логи
tail -f logs/rag_agent.log
```

**Проверка портов:**
```bash
# Если порт 8000 занят
lsof -i :8000
kill -9 <PID>
```

---

### 11. Telegram бот не запускается

**Симптомы:**
```
Invalid token
Connection error
Bot not responding
```

**Решение:**

**Проверка токена:**
```bash
# Формат: 1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
# Получите новый у @BotFather если нужно
```

**Проверка интернета:**
```bash
ping telegram.org
```

**Проверка backend:**
```bash
curl http://localhost:8000/health
```

**Перезапуск:**
1. Settings → Telegram Bot
2. Нажмите "Остановить бота"
3. Подождите 5 секунд
4. Нажмите "Запустить бота"

Подробнее: [TELEGRAM.md](./TELEGRAM.md)

---

### 12. Параметры не применяются

**Важно:** Параметры генерации применяются **только к Ollama**.

Для API моделей используются оптимальные значения:
- **Gemini:** temperature 0.7, max_tokens 2000
- **OpenAI:** temperature 0.7, max_tokens 1000
- **Anthropic:** temperature 0.7, max_tokens 1500

**Если нужно изменить параметры API:**
- Используйте Custom API с вашими настройками
- Или модифицируйте `src/api_model_connector.py`

---

### 13. Ошибка 422 при настройке API

**Симптомы:**
```
POST /models/api/configure 422 (Unprocessable Content)
```

**Причина:** Неправильная валидация данных (исправлено в v2.2.3+).

**Решение:**
```bash
# Обновить проект
git pull
pip install -r requirements.txt

# Перезапустить
./stop.sh && ./start.sh
```

---

### 14. Прогресс-бар не показывается

**Причина:** Старая версия или проблемы с streaming.

**Решение:**
```bash
# Обновить до последней версии
git pull

# Перезапустить
./stop.sh && ./start.sh

# Очистить кэш браузера
Ctrl+Shift+R (или Cmd+Shift+R на Mac)
```

---

### 15. Ошибка памяти (Out of Memory)

**Симптомы:**
```
Killed
Out of memory
MemoryError
```

**Решение:**

**Для Ollama:**
```bash
# Используйте меньшую модель
ollama pull llama3.2:1b  # вместо 7b или 13b

# Уменьшите Context Size
Settings → System → Context Size: 512
```

**Для системы:**
```bash
# Закройте другие приложения
# Увеличьте swap (Linux)
sudo fallocate -l 4G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile
```

---

## 🔍 Расширенная отладка

### Включить детальные логи

```bash
# В .env
LOG_LEVEL=DEBUG

# Перезапустить
./stop.sh && ./start.sh

# Смотреть логи
tail -f logs/rag_agent.log
```

### Проверить все компоненты

```bash
# Health check
curl http://localhost:8000/health | python -m json.tool

# Settings
curl http://localhost:8000/settings | python -m json.tool

# Models
curl http://localhost:8000/models/list | python -m json.tool

# Stats
curl http://localhost:8000/stats | python -m json.tool

# Documents
curl http://localhost:8000/documents | python -m json.tool
```

### Тестовый запрос

```bash
# Загрузить документ
curl -X POST "http://localhost:8000/upload" \
  -F "file=@test.pdf"

# Задать вопрос
curl -X POST "http://localhost:8000/query" \
  -H "Content-Type: application/json" \
  -d '{"question": "Test question"}'
```

### Проверить ChromaDB

```bash
# Проверить базу данных
ls -la data/chroma_db/

# Очистить базу (осторожно!)
rm -rf data/chroma_db/
# Перезагрузите документы после этого
```

---

## 📊 Мониторинг производительности

### Системные ресурсы

```bash
# CPU и память
top
htop

# Диск
df -h
du -sh data/

# Процессы
ps aux | grep python
ps aux | grep ollama
ps aux | grep node
```

### Логи

```bash
# Backend логи
tail -f logs/rag_agent.log

# Ollama логи (macOS)
tail -f ~/.ollama/logs/server.log

# Ollama логи (Linux)
journalctl -u ollama -f
```

---

## 🆘 Полный сброс

Если ничего не помогает:

```bash
# 1. Остановить всё
./stop.sh
killall ollama
killall python
killall node

# 2. Очистить данные (опционально)
rm -rf data/chroma_db/
rm -rf data/documents/*
rm data/settings.json

# 3. Переустановить зависимости
pip install -r requirements.txt --force-reinstall
cd frontend && npm install && cd ..

# 4. Перезапустить Ollama
ollama serve &
ollama pull llama3.2:1b

# 5. Запустить проект
./start.sh
```

---

## 📚 Дополнительные ресурсы

- [INSTALLATION.md](./INSTALLATION.md) - Установка с нуля
- [MODELS.md](./MODELS.md) - Управление моделями
- [TELEGRAM.md](./TELEGRAM.md) - Настройка Telegram бота
- [FAQ.md](./FAQ.md) - Частые вопросы

---

## 💬 Поддержка

Если проблема не решена:

1. **Проверьте документацию** - возможно, ответ уже есть
2. **Проверьте логи** - `logs/rag_agent.log`
3. **Создайте Issue** на GitHub с:
   - Описанием проблемы
   - Шагами для воспроизведения
   - Логами (без чувствительных данных)
   - Версией системы (`git log -1`)

---

**Последнее обновление:** Декабрь 2024
