# 🔧 Исправление: Отображение модели из настроек

## Проблема

Название модели в Header отображалось из `config.py` (значение по умолчанию), а не из текущих настроек пользователя (`settings.json`).

**Было:**
- Header всегда показывал `llama3.2:3b` (из config.py)
- Даже если пользователь выбрал другую модель в настройках

**Ожидалось:**
- Header должен показывать текущую активную модель
- Модель из `settings.json` или API модель

## Причина

В endpoint `/stats` использовалось значение из `config.OLLAMA_MODEL`:

```python
return {
    "model": config.OLLAMA_MODEL,  # ❌ Всегда значение по умолчанию
    ...
}
```

## Решение

Обновлён endpoint `/stats` для получения модели из настроек:

```python
# Получаем текущую модель из настроек
current_model = settings_manager.get('model', config.OLLAMA_MODEL)

# Если используется API модель, показываем её
if settings_manager.get('use_api_model', False):
    api_config = settings_manager.get('api_model_config')
    if api_config:
        current_model = f"{api_config.get('api_type', 'API')}: {api_config.get('model_name', 'Unknown')}"

return {
    "model": current_model,  # ✅ Текущая активная модель
    ...
}
```

## Изменения

### api/server.py

**Endpoint `/stats`:**

1. **Ollama модель:**
   - Берётся из `settings_manager.get('model')`
   - Fallback на `config.OLLAMA_MODEL` если не задана

2. **API модель:**
   - Проверяется `use_api_model`
   - Формат: `"api_type: model_name"`
   - Примеры:
     - `"openai: gpt-4o"`
     - `"anthropic: claude-3-5-sonnet-20241022"`
     - `"gemini: gemini-2.0-flash-exp"`

3. **Top K Results:**
   - Также обновлено для использования `settings_manager`
   - Берётся из `context_length` настроек

## Примеры

### Ollama модель

**Настройки:**
```json
{
  "model": "phi3:mini",
  "use_api_model": false
}
```

**Ответ `/stats`:**
```json
{
  "model": "phi3:mini",
  ...
}
```

**Header отображает:**
```
🤖 phi3:mini
```

### API модель (OpenAI)

**Настройки:**
```json
{
  "use_api_model": true,
  "api_model_config": {
    "api_type": "openai",
    "model_name": "gpt-4o"
  }
}
```

**Ответ `/stats`:**
```json
{
  "model": "openai: gpt-4o",
  ...
}
```

**Header отображает:**
```
🤖 openai: gpt-4o
```

### API модель (Anthropic)

**Настройки:**
```json
{
  "use_api_model": true,
  "api_model_config": {
    "api_type": "anthropic",
    "model_name": "claude-3-5-sonnet-20241022"
  }
}
```

**Ответ `/stats`:**
```json
{
  "model": "anthropic: claude-3-5-sonnet-20241022",
  ...
}
```

**Header отображает:**
```
🤖 anthropic: claude-3-5-sonnet-20241022
```

## Проверка

### 1. Проверить текущую модель

```bash
curl http://localhost:8000/stats | python3 -m json.tool
```

**Ожидаемый результат:**
```json
{
    "model": "phi3:mini",  // или ваша текущая модель
    ...
}
```

### 2. Изменить модель

1. Откройте: **Settings** → **⚙️ System**
2. Выберите другую модель
3. Нажмите **"Сохранить настройки"**
4. Обновите страницу

**Результат:**
- Header должен показать новую модель

### 3. Переключиться на API

1. Откройте: **Settings** → **⚙️ System** → **API Configuration**
2. Выберите тип API (OpenAI, Anthropic, Gemini)
3. Введите API Key и модель
4. Нажмите **"Подключить API"**
5. Обновите страницу

**Результат:**
- Header должен показать: `api_type: model_name`

## Дополнительные улучшения

### Top K Results

Также обновлено для использования настроек:

**Было:**
```python
"top_k_results": config.TOP_K_RESULTS
```

**Стало:**
```python
"top_k_results": settings_manager.get('context_length', config.TOP_K_RESULTS)
```

Теперь отображается актуальное значение из настроек.

## Преимущества

### Для пользователей

1. **Актуальная информация**
   - Header всегда показывает текущую модель
   - Видно какая модель используется

2. **Прозрачность**
   - Понятно работает Ollama или API
   - Видно название API модели

3. **Удобство**
   - Не нужно заходить в настройки
   - Информация всегда на виду

### Для разработчиков

1. **Консистентность**
   - Единый источник правды (settings_manager)
   - Нет расхождений между config и settings

2. **Гибкость**
   - Легко добавить другие параметры
   - Централизованное управление

## Связанные изменения

### Файлы

- `api/server.py` - Обновлён endpoint `/stats`

### Endpoints

- `GET /stats` - Возвращает текущую модель из настроек

### Frontend

- `Header.tsx` - Отображает `stats.model`
- `useStats.ts` - Hook для получения статистики

## Тестирование

### Сценарий 1: Ollama модель

1. Выберите модель в настройках
2. Сохраните
3. Проверьте Header
4. ✅ Должна отображаться выбранная модель

### Сценарий 2: API модель

1. Подключите API модель
2. Проверьте Header
3. ✅ Должна отображаться: `api_type: model_name`

### Сценарий 3: Переключение

1. Используйте Ollama модель
2. Переключитесь на API
3. Проверьте Header
4. ✅ Должна обновиться информация

### Сценарий 4: Отключение API

1. Используйте API модель
2. Отключите API
3. Проверьте Header
4. ✅ Должна вернуться Ollama модель

## Возможные проблемы

### Модель не обновляется

**Причина:** Кэш браузера

**Решение:**
```bash
# Обновить страницу
Ctrl+Shift+R (или Cmd+Shift+R на Mac)
```

### Показывает старую модель

**Причина:** Backend не перезапущен

**Решение:**
```bash
./stop.sh && ./start.sh
```

### API модель не отображается

**Причина:** Неправильная конфигурация

**Решение:**
1. Проверьте настройки API
2. Убедитесь что `use_api_model: true`
3. Проверьте `api_model_config`

## Статус

✅ **Исправлено**
- Модель берётся из настроек
- Поддержка Ollama и API моделей
- Актуальное отображение в Header
- Backend перезапущен

## Следующие шаги

1. ✅ Backend перезапущен
2. ✅ Изменения применены
3. 🎯 Обновите страницу в браузере
4. ✅ Проверьте отображение модели в Header

---

**Дата исправления:** Декабрь 2024  
**Файлы:** api/server.py  
**Статус:** ✅ Решено
