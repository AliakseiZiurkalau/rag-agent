# Веб-интерфейс RAG Agent

## Обзор

Современный веб-интерфейс для взаимодействия с RAG агентом. Построен на чистом JavaScript без фреймворков для минимального размера и быстрой загрузки.

## Возможности

### 📤 Загрузка документов
- Drag & Drop интерфейс
- Поддержка PDF и DOCX файлов
- Валидация размера (макс. 10MB)
- Визуальная обратная связь о статусе загрузки
- Отображение количества созданных чанков

### 💬 Чат с ассистентом
- Интуитивный интерфейс чата
- Автоматическая прокрутка к новым сообщениям
- Индикатор загрузки при обработке запроса
- Отображение количества источников для каждого ответа
- Поддержка многострочного ввода (Shift+Enter)

### 📊 Мониторинг системы
- Статус подключения в реальном времени
- Количество загруженных документов
- Настройки системы (Top-K, размер чанков)
- Размер кэша запросов

## Архитектура

```
static/
├── index.html      # Главная страница
├── style.css       # Стили (CSS переменные, адаптивный дизайн)
├── app.js          # Логика приложения
└── favicon.svg     # Иконка
```

### Технологии

- **Vanilla JavaScript** - без зависимостей
- **CSS Grid & Flexbox** - адаптивная верстка
- **CSS Variables** - легкая кастомизация темы
- **Fetch API** - асинхронные запросы к API
- **FileReader API** - обработка файлов

## Использование

### Запуск

```bash
# С Docker
make up
make test-web

# Без Docker
python main.py
```

Откройте http://localhost:8000 в браузере.

### Загрузка документов

1. Перетащите файл в область загрузки
2. Или нажмите "выберите файл" для выбора через диалог
3. Дождитесь завершения обработки
4. Статистика обновится автоматически

### Общение с ассистентом

1. Введите вопрос в текстовое поле
2. Нажмите Enter или кнопку отправки
3. Дождитесь ответа (появится индикатор загрузки)
4. Просмотрите ответ и количество использованных источников

## Кастомизация

### Изменение цветовой схемы

Отредактируйте CSS переменные в `static/style.css`:

```css
:root {
    --primary: #2563eb;        /* Основной цвет */
    --success: #10b981;        /* Цвет успеха */
    --error: #ef4444;          /* Цвет ошибки */
    --bg: #f8fafc;            /* Фон */
    --surface: #ffffff;        /* Поверхности */
    --text: #1e293b;          /* Текст */
}
```

### Изменение размеров

```css
.chat-container {
    height: 500px;  /* Высота чата */
}

.main-content {
    grid-template-columns: 400px 1fr;  /* Ширина панелей */
}
```

## API Endpoints

Веб-интерфейс использует следующие endpoints:

- `GET /health` - проверка здоровья системы
- `GET /stats` - статистика
- `POST /upload` - загрузка документа
- `POST /query` - запрос к ассистенту
- `DELETE /clear` - очистка базы данных

## Адаптивность

Интерфейс адаптируется под разные размеры экрана:

- **Desktop (>1024px)**: двухколоночная раскладка
- **Tablet/Mobile (<1024px)**: одноколоночная раскладка
- Все элементы масштабируются пропорционально

## Безопасность

- Валидация типов файлов на клиенте
- Проверка размера файлов
- Экранирование HTML в сообщениях
- CORS настраивается через .env файл

## Производительность

- Минимальный размер (HTML + CSS + JS < 20KB)
- Нет внешних зависимостей
- Lazy loading для изображений
- Оптимизированные анимации (CSS transforms)
- Debouncing для автоматического изменения высоты textarea

## Браузерная совместимость

Поддерживаются все современные браузеры:
- Chrome/Edge 90+
- Firefox 88+
- Safari 14+
- Opera 76+

## Troubleshooting

### Интерфейс не загружается

Проверьте, что:
1. Сервер запущен: `curl http://localhost:8000/health`
2. Статические файлы доступны: `ls static/`
3. Нет ошибок в консоли браузера (F12)

### Файлы не загружаются

1. Проверьте размер файла (макс. 10MB)
2. Убедитесь, что формат поддерживается (PDF, DOCX)
3. Проверьте логи: `make logs-rag`

### Ассистент не отвечает

1. Проверьте статус Ollama: `docker-compose exec ollama ollama list`
2. Убедитесь, что модель загружена: `make pull-model`
3. Проверьте, что документы загружены: `curl http://localhost:8000/stats`

## Разработка

### Локальная разработка

```bash
# Запуск с hot-reload
cp docker-compose.override.yml.example docker-compose.override.yml
docker-compose up
```

### Отладка

Откройте консоль браузера (F12) для просмотра:
- Network requests
- Console logs
- JavaScript errors

### Тестирование

```bash
# Тест API
./scripts/test_api.sh

# Ручное тестирование
curl http://localhost:8000/health
```

## Roadmap

Планируемые улучшения:
- [ ] Темная тема
- [ ] История чата (localStorage)
- [ ] Экспорт диалогов
- [ ] Markdown рендеринг в ответах
- [ ] Поддержка изображений в документах
- [ ] Голосовой ввод
- [ ] Мультиязычность интерфейса
